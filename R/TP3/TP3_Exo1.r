x=c(1,5,4,15,78,45,14,25,36,96,23)
#sum(x) #Somme des elements
#length(x) #nombre des elements

#mean(x) #moyenne du c
moy=sum(x)/length(x) #moyenne du c

sd(x) #ecart type de c

median(x) #medianne
var(x) #variance de c
variance=(sum((x-moy)*(x-moy)))/
variance
summary(x) #une resume de x