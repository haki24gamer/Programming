#include <stdio.h>

int main()
{
    int i, j, n, temp,ins,cpt=0;

    //Remplir le tableau
    printf("Entrer la taille du tableau: ");
    scanf("%d",&n);
    int T[n];
    for (i=0;i<n;i++)
    {
        scanf("%d",&T[i]);
    }

    for (i = 0; i < n; i++)
    {
        /* code */
    }
    
}