#include <stdio.h>
#include <string.h>

int main(){
    char nom[50];
    char prenom[50];
    char adress[50];

    gets(nom);
    gets(prenom);


    scanf("");

    printf(nom);
    printf(prenom);
    printf(adress);
    return 0;
}